## Notebooks

This directory is reserved for Jupyter notebooks used for:

- Quick data inspection and visualization.
- Prototyping feature engineering ideas.
- Comparing model variants interactively.

All core logic should live in `ml/scripts/` so that it can be reused from both notebooks and command-line workflows.

